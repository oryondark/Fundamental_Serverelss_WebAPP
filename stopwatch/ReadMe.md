## Stopwatch

```python
w = Watch(td=True)
def fibonacci_test(r):
	for i in range(0, r):
		for j in range(0, r):
			j

w.watch(fibonacci_test, [10000], name='fibo_test')
```
### Create by Hyunjune KIM.
